# Test environment - uses demo data instead of live API
# Copy this to .env.local to use demo mode:
#   cp .env.test .env.local

# Enable demo mode to use synthetic data without real API credentials
DEMO_MODE=true
NEXT_PUBLIC_DEMO_MODE=true

# No real credentials needed in demo mode
# BLUMIRA_CLIENT_ID=
# BLUMIRA_CLIENT_SECRET=
